@model Wolfpack.Web.Models.Event.RenameTeamVM

@using (Html.BeginForm("RenameTeam", "Event"))
{
    <div>
        @Html.EditorFor(m => m.Name)
        @Html.HiddenFor(m => m.Id)
    </div>
    <div>
        <input type="submit" />
    </div>
}